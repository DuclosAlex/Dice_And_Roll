const userModel = require('./userModel');
const characterModel = require('./characterModel');
const statsModel = require('./statsModel');

module.exports = {
    userModel, characterModel, statsModel,
}